import { Event } from '@/types/Event';
import { Server } from '@/types/Server';

export async function drawEventStagePresent(eventId: number, server: Server, compress: boolean): Promise<Array<Buffer | string>> {
    const result = []
    return result;
}